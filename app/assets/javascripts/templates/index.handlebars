<h1>Hej {{studentNumber}}</h1>

{{input type='text' value=searchQuery placeholder='Search for courses on DTU...'}}

<h2>Courses chosen</h2>
{{#each coursesChosen}}
  <div>{{title}}</div>
{{/each}}

{{type-ahead selection=selectedCourse remote=autocompleteRemote}}

Selected: {{selectedCourse.title}}
