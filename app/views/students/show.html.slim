javascript:
  var DTUStudentId     = #{current_student.id.to_json.html_safe};
  var DTUStudentNumber = #{current_student.student_number.to_json.html_safe};
  var MASTER_CATEGORIES = #{CoursePlanning::MASTER_CATEGORIES.to_json.html_safe};
  var BACHELOR_CATEGORIES = #{CoursePlanning::BACHELOR_CATEGORIES.to_json.html_safe};
